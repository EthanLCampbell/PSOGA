from typing import List
# Missle Family Chars

missile_chars = namedtuple('missile_chars', ['name', 'value', 'weight'])

tomahawk_Biv = [
    missile_chars('top_speed',0.7, 100), #altitude
    missile_chars('range', 1000, 100), #moneys
    missile_chars('manouv_radius', 10, 100), #turn radius, ft
    missile_chars('manouv_rate', 10, 100), #turn rate, degree/sec
    missile_chars('cost', 2, 50), #million usd
    missile_chars('precision', 100, 50), #ft^2
    missile_chars('payload', 100, 50) #lbs
]

storm_shadow = [
    missile_chars('top_speed',0.7, 100), #altitude
    missile_chars('range', 1000, 100), #moneys
    missile_chars('manouv_radius', 10, 100), #turn radius, ft
    missile_chars('manouv_rate', 10, 100), #turn rate, degree/sec
    missile_chars('cost', 2, 50), #million usd
    missile_chars('precision', 100, 50), #ft^2
    missile_chars('payload', 100, 50) #lbs
]

agm_harm = [
    missile_chars('top_speed',0.7, 100), #altitude
    missile_chars('range', 1000, 100), #moneys
    missile_chars('manouv_radius', 10, 100), #turn radius, ft
    missile_chars('manouv_rate', 10, 100), #turn rate, degree/sec
    missile_chars('cost', 2, 50), #million usd
    missile_chars('precision', 100, 50), #ft^2
    missile_chars('payload', 100, 50) #lbs
]

p800_rus = [
    missile_chars('top_speed',0.7, 100), #altitude
    missile_chars('range', 1000, 100), #moneys
    missile_chars('manouv_radius', 10, 100), #turn radius, ft
    missile_chars('manouv_rate', 10, 100), #turn rate, degree/sec
    missile_chars('cost', 2, 50), #million usd
    missile_chars('precision', 100, 50), #ft^2
    missile_chars('payload', 100, 50) #lbs
]

zircon_rus = [
    missile_chars('top_speed',0.7, 100), #altitude
    missile_chars('range', 1000, 100), #moneys
    missile_chars('manouv_radius', 10, 100), #turn radius, ft
    missile_chars('manouv_rate', 10, 100), #turn rate, degree/sec
    missile_chars('cost', 2, 50), #million usd
    missile_chars('precision', 100, 50), #ft^2
    missile_chars('payload', 100, 50) #lbs
]

df_zf_china = [
    missile_chars('top_speed',0.7, 100), #altitude
    missile_chars('range', 1000, 100), #moneys
    missile_chars('manouv_radius', 10, 100), #turn radius, ft
    missile_chars('manouv_rate', 10, 100), #turn rate, degree/sec
    missile_chars('cost', 2, 50), #million usd
    missile_chars('precision', 100, 50), #ft^2
    missile_chars('payload', 100, 50) #lbs
]

avangard_rus = [
    missile_chars('top_speed',0.7, 100), #altitude
    missile_chars('range', 1000, 100), #moneys
    missile_chars('manouv_radius', 10, 100), #turn radius, ft
    missile_chars('manouv_rate', 10, 100), #turn rate, degree/sec
    missile_chars('cost', 2, 50), #million usd
    missile_chars('precision', 100, 50), #ft^2
    missile_chars('payload', 100, 50) #lbs
]

c_hcb = [
    missile_chars('top_speed',0.7, 100), #altitude
    missile_chars('range', 1000, 100), #moneys
    missile_chars('manouv_radius', 10, 100), #turn radius, ft
    missile_chars('manouv_rate', 10, 100), #turn rate, degree/sec
    missile_chars('cost', 41, 50), #million usd
    missile_chars('precision', 100, 50), #ft^2
    missile_chars('payload', 100, 50) #lbs
]
